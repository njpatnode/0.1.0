
###
### controller_id - an ID for a column filter control
### new_select_state - changes the new select state from the current select state
def column_filter(controller_id, new_select_state)
    #### raise error if not a column filter type
    